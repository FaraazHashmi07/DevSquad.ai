# DevSquad.ai API Configuration Template
# Copy this file to .env and replace the placeholder values with your actual API keys

# Together AI API Configuration - Dual API Setup
# Emma (Business Analyst) - Gemma API for PRDs and presentations
GEMMA_API_KEY=your_gemma_api_key_here

# Other Agents (Bob, David, Alex, DevOps) - Mistral API
TOGETHER_API_KEY=your_together_ai_api_key_here

# Server Configuration
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Optional: Max file size limit (in bytes, default is 5MB)
MAX_FILE_SIZE=5242880

# Optional: Max number of versions per project
MAX_VERSIONS=10

# Optional: Max number of projects to store
MAX_PROJECTS=100

# API Configuration Notes:
# 1. Get your Together AI API keys from: https://api.together.xyz/
# 2. Both GEMMA_API_KEY and TOGETHER_API_KEY are required for full functionality
# 3. Emma uses Gemma models for business analysis and presentations
# 4. Other agents (Bob, David, Alex, DevOps) use Mistral models
# 5. Without valid API keys, the agents will not be able to generate content

# Security Notes:
# - Never commit your actual API keys to version control
# - Keep your .env file in .gitignore
# - Rotate your API keys regularly for security